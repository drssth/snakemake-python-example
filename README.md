# snakemake-python-example
a simple example of snakemake pipeine: read data --> process data --> visualize

## run the pipeline with `snakemake plot/scatter.png --cores all`, and it will consume all cpu cores.
